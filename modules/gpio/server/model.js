/**
 * ## Model for gpio
 *
 * @module gpio/model
 */

'use strict';

let data = { modelData: 'gpio data' };

module.exports = {
  /**
   * get data
   *
   * @returns {object} model data
   */
  getData: () => {
    return data;
  }
};
