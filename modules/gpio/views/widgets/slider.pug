//- widgets/slider.pug
div(
  class=item.type + '_controls gpio-item-slider-wrapper ' + color
)
  input.gpio-item-slider&attributes({
    id: name.replace(/[^A-zA-Z0-9]/, '_') + (item.type == 'RGBLED' ? '_' + color : '') + '_slider',
    type: 'range',
    name: name.replace(/[^A-zA-Z0-9]/, '_') + (item.type == 'RGBLED' ? '_' + color : '') + '_slider',
    min: item.range.min,
    max: item.range.max,
    value: 0,
    'data-group': group,
    'data-name': name,
    'data-color': color,
    'data-type': item.type
  })
